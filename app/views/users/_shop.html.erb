<%= form_for(current_user.user_courses_relationships.build( course_id: course.id )) do |f| %>
  <div><%= f.hidden_field :user_id %></div>
  <div><%= f.hidden_field :course_id %></div>
  <%= f.submit "Shop Course", class: "btn btn-primary" %>
<% end %>