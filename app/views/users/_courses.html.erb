<% @result.each do |course| %>

<tr>
 <H3><%= course.name %></H3>
 <h4><%=h course.institute%></h4>
		<div class="description">
			<%= truncate(strip_tags(sanitize(course.description)), length: 500)%>
</br>
<h4>
	Staff: <%= course.staff_name %>	
</h4>

<%= render 'shop' , :course => course %>

</br>



<%end%>
                